# LaTeX Build Base

A CI/CD component for building LaTeX documents.

This project is a GitLab CI/CD component which allows for reuse of LaTeX build jobs in GitLab pipelines.

## Templates

The following projects are included with this component.

### latex-build

Uses the latest `texlive` Docker image to build a LaTeX document.